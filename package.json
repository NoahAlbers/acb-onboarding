{
  "name": "acb-onboarding",
  "version": "1.0.0",
  "description": "Advanced Collection Bureau client onboarding portal — one shareable link per client, one collection agreement per legal entity, e-sign them all at once.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "express": "^5.2.1",
    "multer": "^2.2.0",
    "pdf-lib": "^1.17.1"
  }
}
